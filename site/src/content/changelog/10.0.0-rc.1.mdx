---
description: "Adds Wistia playback, installable skins, and a dedicated CDN package."
date: 2026-09-08
version: "10.0.0-rc.1"
prerelease: true
breaking: true
compareUrl: "https://github.com/videojs/v10/compare/@videojs/core@10.0.0-beta.32...@videojs/core@10.0.0-rc.1"
---

Video.js 10 reaches its first release candidate with a new Wistia player, installable generated skins, and a cleaner split between playback integrations, extensions, and CDN bundles.

## Breaking changes

- **Playback integrations are standalone packages** ([#2567](https://github.com/videojs/v10/pull/2567)). Install the adapter alongside your framework package, such as `pnpm add @videojs/react @videojs/mux-video`; framework façade imports remain the same. Replace removed `@videojs/media/dom/*` imports with `@videojs/media` or `@videojs/media/dom` as appropriate.
- **Google Cast and Mux Data use extension paths** ([#2577](https://github.com/videojs/v10/pull/2577)). Change `@videojs/react/media/google-cast` to `@videojs/react/extensions/google-cast`, and make the equivalent `media` → `extensions` change for Mux Data, HTML, and CDN imports.
- **Extension APIs use the `Extension` name** ([#2600](https://github.com/videojs/v10/pull/2600), [#2601](https://github.com/videojs/v10/pull/2601)). Rename `MuxData` → `MuxDataExtension`, `GoogleCast` → `GoogleCastExtension`, `MediaComponent` → `MediaExtension`, and `useMediaComponent` → `useMediaExtension`; move standalone default-prop objects to each class's `defaultProps`.
- **Playback classes are now adapters** ([#2602](https://github.com/videojs/v10/pull/2602)). Rename classes and props such as `HlsJsMedia` → `HlsJsAdapter` and `HlsJsMediaProps` → `HlsJsAdapterProps`, use class `defaultProps` instead of `*MediaDefaultProps`, and update extension calls from `setMedia` to `setAdapter`.
- **Browser bundles moved to `@videojs/cdn`** ([#2598](https://github.com/videojs/v10/pull/2598)). Replace `@videojs/html@<version>/cdn/...` URLs with `@videojs/cdn@<version>/...`; media and extensions remain under their `media/` and `extensions/` paths.

## More ways to play

Wistia is now a first-class media option. Use `<wistia-video>` in HTML or `<WistiaVideo>` in React, pass a Wistia URL or hashed ID as `src`, and send Wistia-specific options through `source` ([#2305](https://github.com/videojs/v10/pull/2305)). Read the reference for [HTML](/docs/framework/html/reference/wistia-video/) and [React](/docs/framework/react/reference/wistia-video/).

The CDN is now its own package, with browser-ready bundles and stylesheets in one place ([breaking, above](#breaking-changes)). The new published stylesheets let you style a player before its custom elements upgrade, and are covered in the [CDN documentation](/docs/framework/html/concepts/cdn/) ([#2340](https://github.com/videojs/v10/pull/2340)).

## Skins you can take with you

Default and Minimal skins now come from one VJSC source, so the HTML, React, CSS, and Tailwind variants share the same component and styling model ([#2479](https://github.com/videojs/v10/pull/2479), [#2527](https://github.com/videojs/v10/pull/2527), [#2545](https://github.com/videojs/v10/pull/2545), [#2546](https://github.com/videojs/v10/pull/2546)). Menu state follows options across nested targets, which keeps controls consistent as available choices change ([#2528](https://github.com/videojs/v10/pull/2528)).

You can now install and edit those skins through the hosted Shadcn registry instead of copying generated scripts ([#2544](https://github.com/videojs/v10/pull/2544), [#2547](https://github.com/videojs/v10/pull/2547), [#2615](https://github.com/videojs/v10/pull/2615)). Start with [Customize skins](/docs/framework/html/how-to/customize-skins/) for HTML or [Customize skins](/docs/framework/react/how-to/customize-skins/) for React. Follow-up work aligned generated skins across frameworks and styling modes, including popovers, menus, tooltips, dialogs, sliders, and player feedback ([#2530](https://github.com/videojs/v10/pull/2530), [#2576](https://github.com/videojs/v10/pull/2576), [#2580](https://github.com/videojs/v10/pull/2580)).

## Player polish

Error dialogs now stay inside their player, preserving the rest of the page, and handle narrow layouts without hiding the dismissal control ([#2449](https://github.com/videojs/v10/pull/2449), [#2451](https://github.com/videojs/v10/pull/2451)). Posters with no source no longer draw broken-image chrome ([#2453](https://github.com/videojs/v10/pull/2453)), and seek thumbnails fill their preview box ([#2517](https://github.com/videojs/v10/pull/2517)).

Keyboard and pointer interactions are steadier too: fullscreen keeps working after a pointer activation followed by Space, a focused slider owns its arrow keys and keeps controls visible while pressed, invalid gesture types no longer fall back to tap, and holding a volume key at its limit no longer repeats feedback ([#2472](https://github.com/videojs/v10/pull/2472), [#2473](https://github.com/videojs/v10/pull/2473), [#2474](https://github.com/videojs/v10/pull/2474), [#2475](https://github.com/videojs/v10/pull/2475), [#2505](https://github.com/videojs/v10/pull/2505)). The hls.js AirPlay bridge also respects an author-supplied `disableRemotePlayback` preference ([#1889](https://github.com/videojs/v10/pull/1889)).

## Playback reliability

The SPF engine now retains the configured preload value on attach, can check codec support through `ManagedMediaSource` where classic MSE is unavailable, and avoids a waiting state that never clears ([#2534](https://github.com/videojs/v10/pull/2534), [#2564](https://github.com/videojs/v10/pull/2564), [#2574](https://github.com/videojs/v10/pull/2574)). Mux Data also keeps one view for each viewing across repeated `loadstart` events ([#2565](https://github.com/videojs/v10/pull/2565)).
